version: '3.4'

services:
  app:
    build:
      args:
        RUN_PRECOMPILATION: 'false'
    volumes:
      - ".:/usr/src/app"
      - "tmp:/usr/src/app/tmp"
      - "storage:/usr/src/app/storage"
      - "node_modules:/usr/src/app/node_modules"

volumes:
  tmp:
  storage:
  node_modules:
