<h3 class="govuk-heading-m"><%= location.address %></h2>
<table class="govuk-table">
  <thead class="govuk-table__head">
    <tr class="govuk-table__row">
      <th class="govuk-table__header" scope="col">IP Address</th>
      <th class="govuk-table__header"></th>
    </tr>
  </thead>
  <tbody class="govuk-table__body">
    <% location.ips.each do |ip| %>
      <tr class="govuk-table__row">
        <td class="govuk-table__cell" scope="row"><%= ip.address %></td>
        <% if ip.available? %>
          <td class="govuk-table__cell">Available</td>
        <% else %>
          <td class="govuk-table__cell unavailable-ip">Activating tomorrow
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
