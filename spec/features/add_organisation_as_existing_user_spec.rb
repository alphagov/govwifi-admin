require 'rack_session_access/capybara'

describe 'Adding an organisation as exisiting user', type: :feature do
  let(:organisation_1) { create(:organisation) }
  let(:organisation_2) { create(:organisation) }
  let(:user) { create(:user) }


end
